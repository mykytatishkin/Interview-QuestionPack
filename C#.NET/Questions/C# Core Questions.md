---
title: C# Core Questions — Вопросы по C# Core
tags:
  - csharp
  - questions
  - interview
---

# C# Core Questions

**Навигация:** [[../README|← C#.NET]] · [[../C#/README|C# Topics]] · [[../Generated tests/csharp_test|Tests]]

---

Актуальные вопросы для собеседований по C# Core на 2025-2026 год.

## 📋 Вопросы по основам C#

### 1. Типы данных
- В чем разница между value types и reference types?
- Что такое boxing и unboxing?
- Когда использовать `decimal` вместо `double`?
- Что такое nullable reference types (C# 8.0+)?

### 2. Переменные и операторы
- Когда использовать `var`, а когда явный тип?
- В чем разница между `ref` и `out` параметрами?
- Что такое `params` параметр?
- Как работает оператор `??` (null-coalescing)?

### 3. Управляющие конструкции
- В чем разница между `for`, `while` и `foreach`?
- Когда использовать `switch` expression (C# 8.0+)?
- Что такое pattern matching в C#?

### 4. Коллекции
- В чем разница между `List<T>` и массивом?
- Когда использовать `Dictionary` vs `HashSet`?
- Что такое `IEnumerable` и `IEnumerator`?
- Как работает LINQ?

---

## 📋 Вопросы по ООП

### 1. Основные принципы
- Объясни инкапсуляцию, наследование, полиморфизм
- В чем разница между `virtual` и `abstract`?
- Когда использовать `sealed` класс?
- Что такое `partial` класс?

### 2. Наследование
- Можно ли наследовать от нескольких классов в C#?
- В чем разница между `override` и `new`?
- Что такое конструктор копирования?
- Как работает `base` и `this`?

### 3. Интерфейсы
- В чем разница между интерфейсом и абстрактным классом?
- Можно ли иметь реализацию в интерфейсе (C# 8.0+)?
- Что такое явная реализация интерфейса?
- Когда использовать интерфейсы для DI?

---

## 📋 Вопросы по Generics

- Что такое generics и зачем они нужны?
- В чем разница между `where T : class` и `where T : struct`?
- Что такое ковариантность и контравариантность?
- Можно ли использовать generics с nullable типами?

---

## 📋 Вопросы по Reflection

- Что такое reflection и когда его использовать?
- Как создать экземпляр класса по имени?
- Что такое атрибуты и как их использовать?
- В чем разница между `Type.GetType()` и `typeof()`?

---

## 📋 Вопросы по Memory Management

- Как работает Garbage Collector в .NET?
- Что такое поколения объектов (generations)?
- Когда использовать `IDisposable`?
- В чем разница между `using` и `try-finally`?
- Что такое финализаторы и когда их использовать?

---

## 💡 Как использовать

1. Изучи теорию в [[../Topics/C# Basics|Topics]]
2. Ответь на вопросы выше
3. Проверь знания в [[../Generated tests/csharp_test|Generated Tests]]

---

> [!tip] Совет
> Каждый вопрос связан с соответствующей темой в Topics. Используй wiki-links для быстрого перехода к теории.
