---
title: Java Core Summary — Шпаргалка по Java Core
tags:
  - java
  - core
  - topic
  - summary
parent: [[../README|Topics]]
child: [[quick_check|Quick Check]]
breadcrumbs:
  - [[../../Java 2025-2026|Java]]
  - [[../README|Topics]]
  - [[notes|Java Core]]
---

# Java Core Summary — Шпаргалка по Java Core

**Навигация:** [[../README|← Topics]]

---

Краткая выжимка по всем темам Java Core с быстрым опросником.

## 📚 Теория

### 1. hashCode() и equals()

**Контракт:**
- Если два объекта равны по `equals()`, у них обязан быть одинаковый `hashCode()`
- Обратное неверно: одинаковый `hashCode` не значит, что объекты равны

**Свойства equals():**
- Рефлексивность: `a.equals(a)` → true
- Симметричность: `a.equals(b)` ⟺ `b.equals(a)`
- Транзитивность: `a.equals(b)` ∧ `b.equals(c)` ⇒ `a.equals(c)`
- Консистентность: повторные вызовы без изменений дают тот же результат
- Сравнение с `null`: всегда `false`

> [!warning] Частые ошибки
> - Переопределить только `equals` или только `hashCode`
> - Использовать мутабельные объекты как ключи в HashMap

---

### 2. HashMap

**Внутреннее устройство (Java 8+):**
- Массив бакетов (корзин)
- Каждый бакет — связный список
- При ≥8 элементах в бакете и capacity ≥ 64 — красно-чёрное дерево

**Основные параметры:**
- **Load factor** (0.75): при `size ≥ capacity * loadFactor` делается rehash
- Допускается один `null`-ключ, много `null`-значений
- Дубликат ключа → новое значение заменяет старое

**Производительность:**
- В среднем O(1) для get/put
- В худшем случае (много коллизий) — O(n)

> [!warning] Потокобезопасность
> HashMap не потокобезопасен. Используй ConcurrentHashMap для многопоточности.

---

### 3. Concurrency / Multithreading

**Создание потоков:**
- Наследование от `Thread`
- Реализация `Runnable` или `Callable`
- `ExecutorService` (предпочтительно)

**Runnable vs Callable:**
- **Runnable**: не возвращает значение, не бросает checked-исключения
- **Callable**: возвращает результат через `Future`, может бросать checked-исключения

**synchronized vs volatile:**
- **synchronized**: взаимоисключение + гарантии видимости (happens-before)
- **volatile**: только видимость изменений, без блокировок

---

### 4. JMM (Java Memory Model)

**Зачем нужна JMM:**
- Описывает, как потоки взаимодействуют через память
- Видимость записей, переупорядочивание инструкций
- Правила синхронизации

**Happens-before:**
- Program order: действия в одном потоке упорядочены
- Monitor lock: unlock происходит-before последующего lock
- Volatile: запись в `volatile` happens-before последующего чтения
- Thread start: `start()` happens-before любых действий в нити

---

### 5. Java Core

**ООП:** Инкапсуляция, наследование, полиморфизм, абстракция

**Overloading vs Overriding:**
- **Overloading**: одно имя метода, разные сигнатуры. Разрешается на этапе компиляции
- **Overriding**: переопределение метода предка. Разрешается в рантайме

**String Pool:**
- Литералы строк хранятся в heap в общем пуле
- Повторное использование того же литерала даёт ссылку на тот же объект

**Collections:**
- **List**: упорядоченная, с дубликатами (ArrayList, LinkedList)
- **Set**: без дубликатов (HashSet, LinkedHashSet, TreeSet)
- **Map**: пары ключ–значение (HashMap, LinkedHashMap, TreeMap, ConcurrentHashMap)

---

### 6. Java Spring

**IoC (Inversion of Control):**
- Управление объектами и их зависимостями передано контейнеру

**DI (Dependency Injection):**
- Внедрение зависимостей через конструктор, сеттеры или поля

**Spring-бины:**
- Объекты, которые создаёт и управляет контейнер Spring

**Spring Boot:**
- `@SpringBootApplication`: конфигурация + component scan + auto-configuration
- Starters: готовые наборы зависимостей
- Auto-configuration: автоматическая настройка бинов по classpath

---

## 🎯 Быстрый опросник

### Вопрос 1
Что произойдет, если переопределить только `equals()`, но не `hashCode()`?

<details>
<summary>Ответ</summary>

Объекты с одинаковым `equals()` но разным `hashCode()` будут работать некорректно в HashMap/HashSet. Могут быть потеряны или не найдены элементы.
</details>

---

### Вопрос 2
Как работает HashMap при коллизиях?

<details>
<summary>Ответ</summary>

Разные ключи с одинаковым `hashCode` попадают в один бакет. До Java 8 — связный список. С Java 8+ — при ≥8 элементах и capacity ≥ 64 преобразуется в красно-чёрное дерево для O(log n) вместо O(n).
</details>

---

### Вопрос 3
В чем разница между `synchronized` и `volatile`?

<details>
<summary>Ответ</summary>

- `synchronized`: взаимоисключение (только один поток в блоке) + гарантии видимости
- `volatile`: только видимость изменений между потоками, без блокировок
- `volatile` не даёт атомарности составных операций (например, `i++`)
</details>

---

### Вопрос 4
Что такое happens-before в JMM?

<details>
<summary>Ответ</summary>

Happens-before — отношение, которое гарантирует, что если действие A happens-before действия B, то результаты A видны для B. Обеспечивает упорядочивание операций в многопоточности.
</details>

---

### Вопрос 5
В чем разница между `Runnable` и `Callable`?

<details>
<summary>Ответ</summary>

- `Runnable`: не возвращает значение, не бросает checked-исключения
- `Callable`: возвращает результат через `Future`, может бросать checked-исключения
- `Callable` используется с `ExecutorService.submit()`
</details>

---

### Вопрос 6
Что такое String Pool в Java?

<details>
<summary>Ответ</summary>

String Pool — область в heap, где хранятся литералы строк. Повторное использование того же литерала даёт ссылку на тот же объект. Экономит память и позволяет использовать `==` для сравнения литералов.
</details>

---

### Вопрос 7
В чем разница между `@Component`, `@Service`, `@Repository`?

<details>
<summary>Ответ</summary>

Все являются стереотипами Spring, функционально эквивалентны `@Component`:
- `@Component` — общий компонент
- `@Service` — слой сервисов (семантическое различие)
- `@Repository` — слой доступа к данным (исключения → DataAccessException)
- `@Controller` / `@RestController` — слой веб
</details>

---

## 🔗 Связанные темы

- [[Java OOP|ООП в Java]] — следующая тема
- [[Java Collections|Collections]] — коллекции Java
- [[../Generated tests/java_junior_interview_test|Тест по Java]] — проверка знаний

---

## 📖 Рекомендуемый порядок

1. Изучи теорию выше
2. Пройди быстрый опросник
3. Проверь знания в [[../Generated tests/java_junior_interview_test|Generated Tests]]
4. Изучи детальные темы в других файлах Topics
